-- Task 1
CREATE TABLE IF NOT EXISTS database(
    name TEXT,
    version FLOAT,
    download_count INTEGER
);

-- New
create table if not exists database(
    name text,
    version float,
    download_count integer
)